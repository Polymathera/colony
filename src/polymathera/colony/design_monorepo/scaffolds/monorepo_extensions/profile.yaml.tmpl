# Profile "${name}" — authored via L1-E ``bootstrap_profile``.
#
# Lives at .colony/profiles/${name}.yaml. L1-A's ``discover_profiles``
# parses each .yaml under the profile surface and keys the result by
# filename stem. Top-level must be a mapping.
name: ${name}
description: ${description}
tags:${tags_yaml}
embedding_strategy: ${embedding_strategy}
