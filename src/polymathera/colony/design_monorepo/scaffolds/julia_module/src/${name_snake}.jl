"""
    ${name_snake}

${description}

Scaffolded into tools/${purpose}/${name}/ of a Polymathera Colony design
monorepo. Replace the smoke-test ``run`` with the tool's real
implementation.
"""
module ${name_snake}

export run

"Smoke entry point: returns the input value unchanged."
run(x) = x

end # module
