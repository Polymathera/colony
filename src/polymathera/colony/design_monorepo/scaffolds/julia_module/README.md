# ${name}

${description}

Julia module scaffolded into ``tools/${purpose}/${name}/`` of a
Polymathera Colony design monorepo. Replace the smoke surface in
``src/${name_snake}.jl`` with the tool's real implementation; keep the
``test/runtests.jl`` harness so the tool-building pool can run
``julia --project=. -e 'using Pkg; Pkg.test()'`` uniformly.

## Provenance

Scaffolded ${iso_date} by ${author}. Licence: ${license}.
