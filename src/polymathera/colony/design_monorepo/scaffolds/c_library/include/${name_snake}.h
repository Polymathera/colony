/* ${name} — ${description}
 *
 * Scaffolded into tools/${purpose}/${name}/ of a Polymathera Colony
 * design monorepo. Replace this header's symbols with the tool's real
 * public surface.
 *
 * Copyright (c) ${year} ${author}. Licence: ${license}.
 */
#ifndef ${name_snake}_H
#define ${name_snake}_H

#ifdef __cplusplus
extern "C" {
#endif

/* Smoke-test entry point: returns the input value unchanged. The
 * tool-building pool is expected to replace this with the real API. */
int ${name_snake}_echo(int x);

#ifdef __cplusplus
}
#endif
#endif
