# ${name}

${description}

CMake / C++17 project scaffolded into ``tools/${purpose}/${name}/`` of a
Polymathera Colony design monorepo.

## Build & test

```
cmake -S . -B build
cmake --build build
ctest --test-dir build --output-on-failure
```

## Provenance

Scaffolded ${iso_date} by ${author}. Licence: ${license}.
