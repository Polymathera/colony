# Colony API Keys
# Copy this file to .env and fill in your keys:
#   cp .env.template .env
#
# These secrets are automatically passed to Docker containers during
# colony-env up/run — no need to export them in your shell. Operator
# config knobs (PDF extractor backend, image dir, vector-store URL,
# etc.) live in the operator YAML — see configs/example.yaml under
# ``polymathera_cluster.knowledge``.

ANTHROPIC_API_KEY=
OPENROUTER_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=
MISTRAL_API_KEY=
LLAMA_CLOUD_API_KEY=
